# shared

Shared resources used across the AI/ML pipelines and the backend: JSON schemas
(the locked Phase 1/2/3 output contracts), shared data types, and common
utilities such as zone definitions and adjacency logic.

Keeping these in one place prevents duplicate zone logic between `cv_pipeline`
and `risk_engine`.

**Status:** Scaffolding only — empty placeholder (`shared/.gitkeep`).
