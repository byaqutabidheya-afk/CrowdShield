# demo / mock_data

Cached/mock data for the demo:

- Pre-computed social-media sentiment cache (fallback if the live LLM call fails).
- Per-video fine-tuned zone configs (from Phase 8 calibration).
- Pre-generated multilingual announcement audio cache (fallback during the pitch).

**Status:** Scaffolding only — no data present yet.
