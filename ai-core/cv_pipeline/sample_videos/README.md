# sample_videos

Sample crowd videos used to test the CV pipeline offline.

Place 2–3 short clips here covering different crowd conditions (sparse/generic,
dense flowing, bottlenecked) so the pipeline can be validated without live
cameras. Video files are **not tracked in git** (see root `.gitignore`).
